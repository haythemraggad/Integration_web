{
  "name": "integrtaionweb",
  "version": "1.0.0",
  "description": "A responsive medical products e-commerce website UI built with HTML, Tailwind CSS, and JavaScript.",
  "main": "index.js",
  "scripts": {
    "build:css": "tailwindcss -i ./css/tailwind.css -o ./css/styles.css",
    "watch:css": "tailwindcss -i ./css/tailwind.css -o ./css/styles.css --watch",
    "dev": "npm run watch:css",
    "verify": "node build-verification.js",
    "build": "tailwindcss -i ./css/tailwind.css -o ./css/styles.css",
    "vercel-build": "tailwindcss -i ./css/tailwind.css -o ./css/styles.css",
    "serve": "live-server --port=3000",
    "start": "npm run serve",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^4.0.11",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.3"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}
